version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12.x
  build:
    commands:
      - npm install
      - npm run build

artifacts:
  files:
    - dist/portfolio/**/*
  base-directory: dist/portfolio
