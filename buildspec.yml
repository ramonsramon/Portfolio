version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14.20
  build:
    commands:
      - npm install
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: dist/portfolio
